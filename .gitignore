# build files
/paintress
/docgen
# test
coverage.out
# paintress runtime state on target repository
.expedition/
# environment-specific local files (must not contain full paths)
*.local.*
# claude local settings
.claude/*.local.*
# git worktrees
.worktrees/
# plan drafts (ephemeral)
docs/plans/
# golang
.gocache/
# fake-claude binary (not source in testdata/)
/fake-claude
