# OS
.DS_Store
# build artifacts
/dist/
# test
coverage.out
# paintress runtime state — ignore contents individually
# git-tracked: insights/, archive/, journal/, config.yaml (per S0030)
.expedition/.run/
.expedition/events/
.expedition/inbox/
.expedition/outbox/
.expedition/skills/
.expedition/.otel.env
.expedition/.gitignore
# nested .expedition/ dirs from test/dev runs (state dirs created outside repo root)
internal/**/.expedition/
# environment-specific local files (must not contain full paths)
*.local.*
# claude local settings
.claude/*.local.*
# git worktrees
.worktrees/
# plan drafts (ephemeral)
docs/plans/
# golang
.gocache/
# firebase local debug log
firebase-debug.log
